# tests/

**Parity and unit tests** go here. Empty by design.

Every routine migrated from VBA should have a pytest parity test that feeds identical inputs
and asserts equivalence to the original workbook's outputs (golden file).
