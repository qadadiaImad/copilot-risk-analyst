# sql/

Reviewed, parameterized **SQL queries** go here. Empty by design.

Copilot applies `.github/instructions/sql.instructions.md` to any `*.sql` file here, which
requires inspecting the live schema via the SQLcl MCP server before composing a query.
